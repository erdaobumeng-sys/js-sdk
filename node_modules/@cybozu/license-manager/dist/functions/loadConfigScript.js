"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.loadConfigScript = void 0;
const fs_1 = __importDefault(require("fs"));
const path_1 = __importDefault(require("path"));
const loadConfigScript = async (relativeConfigPathArg) => {
    const relativeConfigPath = relativeConfigPathArg || "license-manager.config.js";
    const configPath = path_1.default.join(process.cwd(), relativeConfigPath);
    if (!fs_1.default.existsSync(configPath)) {
        return {};
    }
    console.log(`💡 Detected ${relativeConfigPath}`);
    const requirePath = path_1.default.relative(__dirname, configPath);
    const config = require(requirePath);
    // TODO: need validation
    return config;
};
exports.loadConfigScript = loadConfigScript;
