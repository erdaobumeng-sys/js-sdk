import { Config, Dependency } from "../types";
export declare const aggregate: (packageManager: string, query: string, cwd: string, workspace: string, unreadLicenseTextPattern: Array<string | RegExp>, config: Config) => Promise<Dependency[]>;
