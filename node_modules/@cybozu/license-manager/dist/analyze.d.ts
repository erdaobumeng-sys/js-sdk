export type AnalyzeArgs = {
    workspace: string;
    query: string;
    cwd: string;
    allowLicenses: Array<string | RegExp>;
    allowPackages: Array<string | RegExp>;
    packageManager: string;
};
export declare const analyze: ({ configFilePath, ...cliArgs }: AnalyzeArgs & {
    configFilePath?: string;
}) => Promise<void>;
