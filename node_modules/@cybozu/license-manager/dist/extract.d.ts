export type ExtractArgs = {
    workspace: string;
    query: string;
    cwd: string;
    extractLicenses: Array<string | RegExp>;
    excludePackages: Array<string | RegExp>;
    output: string;
    json: boolean;
    packageManager: string;
};
export declare const extract: ({ configFilePath, ...cliArgs }: ExtractArgs & {
    configFilePath?: string;
}) => Promise<void>;
